var express = require('express');
var router = express.Router();

/* GET users listing. */
//router.get('/upload', function(req, res) {
//  res.render("debug/upload",{title:"上传调试"});
//});

module.exports = router;
